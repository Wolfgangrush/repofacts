---
description: Verify GitHub repos an AI recommended — real, licensed, alive, runnable, safe.
---

Run `repofacts` over the repositories in $ARGUMENTS (a file path, a pasted list, or the repos
mentioned earlier in this conversation).

Use `--deep` if the user asks for security, quality, or "what will installing this do".
Report STOP findings first, in plain words. Never present `unchecked` as a pass.
